<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.abilists.plugins.stock.dao.MStockDao">

	<insert id="istPluginsMStockCompany" parameterType="map">
		INSERT INTO plugins_m_stock_company (
			msc_code,
			msc_name,
			msc_profit,
			msc_dividend,
			msc_payout_ratio,
			msc_comment,
			msc_status,
			msc_delete,
		  	user_id,
			insert_time,
			update_time
		) VALUES (
			#{mscCode},
			#{mscName},
			#{mscProfit},
			#{mscDividend},
			#{mscPayoutRatio},
			#{mscComment},
			'0',
			'0',
			#{userId},
			now(),
			now()
		)
	</insert>

	<insert id="istPluginsUserStock" parameterType="map">
		INSERT INTO plugins_user_stock (
			ust_sale_day,
			ust_classify,
			ust_sale_cost,
			ust_sale_cnt,
			ust_sale_fee,
			ust_stock_cnt,
			ust_comment,
			ust_status,
			ust_delete,
		  	user_id,
		  	msc_no,
			insert_time,
			update_time
		) VALUES (
			#{ustSaleDay},
			#{ustClassify},
			#{ustSaleCost},
			#{ustSaleCnt},
			#{ustSaleFee},
			#{ustStockCnt},
			#{ustComment},
			'0',
			'0',
			#{userId},
			#{mscNo},
			now(),
			now()
		)
	</insert>

	<update id="udtPluginsMStockCompany" parameterType="map">
		UPDATE 
			plugins_m_stock_company
		SET
			msc_code = #{mscCode},
			msc_name = #{mscName},
			msc_profit = #{mscProfit},
			msc_dividend = #{mscDividend},
			msc_payout_ratio = #{mscPayoutRatio},
			msc_comment = #{mscComment},
			update_time = now()
		WHERE
			msc_no = #{mscNo} AND
			user_id = #{userId} AND
			msc_delete = '0'
	</update>

	<update id="udtPluginsUserStock" parameterType="map">
		UPDATE 
			plugins_user_stock
		SET
			ust_sale_day = #{ustSaleDay},
			ust_classify = #{ustClassify},
			ust_sale_cost = #{ustSaleCost},
			ust_sale_cnt = #{ustSaleCnt},
			ust_sale_fee = #{ustSaleFee},
			ust_comment = #{ustComment},
			update_time = now()
		WHERE
			ust_no = #{ustNo} AND
			user_id = #{userId} AND
			ust_delete = '0'
	</update>

	<update id="dltPluginsMStockCompany" parameterType="map">
		UPDATE 
			plugins_m_stock_company
		SET
			ust_delete = '1',
			update_time = now()
		WHERE
			ust_no = #{ustNo}
	</update>

	<update id="dltPluginsUserStock" parameterType="map">
		UPDATE 
			plugins_user_stock
		SET
			ust_delete = '1',
			update_time = now()
		WHERE
			ust_no = #{ustNo}
	</update>

</mapper>